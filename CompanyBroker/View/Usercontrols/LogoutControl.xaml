<UserControl x:Class="CompanyBroker.View.Usercontrols.LogoutControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CompanyBroker.View.Usercontrols"
             mc:Ignorable="d" 
             DataContext="{Binding Logout,Source={StaticResource Locator}}"
             d:DesignHeight="100" d:DesignWidth="400">
    <Grid>
        <!-- definitions -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <!-- content -->
        <Button Grid.Column="0">Account</Button>
        <Button Grid.Column="1">Settings</Button>
        <Button Grid.Column="2" Command="{Binding LogoutCommand}">Logout</Button>
    </Grid>
</UserControl>
